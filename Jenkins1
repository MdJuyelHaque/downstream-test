pipeline {
    agent any
    stages {
        parameters {
        string(name: 'ENV', description: 'enter env value')
    }
        stage('Example') {
            steps {
                script{
                    echo "${params.ENV}"
                }
            }
        }
    }
}
