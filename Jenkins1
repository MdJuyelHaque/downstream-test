pipeline {
    agent any
    parameters {
        string(name: 'ENV', description: 'enter env value',defaultValue:'juyel')
        string(name: 'ENV1', description: 'enter env value',defaultValue:'juyel')
    }
    stages {
                   
        stage('Example') {
            steps {
                script{
                    echo "${params.ENV}"
                    echo "${params.ENV1}"
                }
            }
        }
    }
}
