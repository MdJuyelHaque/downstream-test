pipeline {
    agent any
    stages {
      properties([
        parameters([
                            string( 
                                name: 'ENV', 
                                trim: true
                            )
                        ])
                    ])
        stage('Example') {
            steps {
                script{
                    echo "${params.ENV}"
                }
            }
        }
    }
}
