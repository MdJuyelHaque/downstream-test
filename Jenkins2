pipeline {
    agent any
    stages {
        parameters { string(name: 'ENV', value: 'juyel', description: 'enter') 
        }
        stage("PARAM 1") {
            steps {
                build job: 'job2', parameters: [string(name: 'ENV', value: "${params.ENV}")]
            }
        }
    }
}
