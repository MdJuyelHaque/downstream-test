pipeline {
    agent any
    stages {
        stage("PARAM 1") {
            steps {
                build job: 'job2', parameters: [string(name: 'ENV', value: "${params.ENV}")]
            }
        }
    }
}
